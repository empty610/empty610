// Keep editable sources separate while serving one render-blocking request.
const fs = require('node:fs');
const path = require('node:path');
const root = path.resolve(__dirname, '..');
const sources = ['style.css', 'venus-entry.css', 'navigation.css'];
const css = sources.map(file => fs.readFileSync(path.join(root, file), 'utf8')).join('\n');
fs.writeFileSync(path.join(root, 'home.css'), '/* Generated by scripts/build-home-css.cjs */\n' + css);
